// we have to initize the const variable
let num;

const num2 = 2;

const obj = {
  name: "john"
};
console.log(obj.name);

// we can assign a new value to object
obj.name = "bob";
console.log(obj.name);
